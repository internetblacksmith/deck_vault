<div class="chat-container" data-controller="chat">
  <div class="chat-header">
    <h1>Collection Assistant</h1>
    <p class="chat-subtitle">Ask me about your MTG collection</p>
  </div>

  <div class="chat-messages" data-chat-target="messages">
    <div class="message assistant">
      <div class="message-content">
        Hi! I'm your MTG Collection Assistant. I can help you:
        <ul>
          <li>Check your collection stats</li>
          <li>Find cards you own or are missing</li>
          <li>Search for specific cards</li>
          <li>Update card quantities</li>
        </ul>
        What would you like to know about your collection?
      </div>
    </div>
  </div>

  <div class="chat-input-container">
    <form data-action="submit->chat#send" class="chat-form">
      <input 
        type="text" 
        data-chat-target="input"
        placeholder="Ask about your collection..."
        class="chat-input"
        autocomplete="off"
      >
      <button type="submit" class="chat-send-btn" data-chat-target="sendButton">
        Send
      </button>
    </form>
  </div>

  <input type="hidden" data-chat-target="history" value="[]">
</div>

<style>
  .chat-container {
    display: flex;
    flex-direction: column;
    height: calc(100vh - 120px);
    max-width: 800px;
    margin: 0 auto;
    background: #1a1a1a;
    border-radius: 8px;
    overflow: hidden;
  }

  .chat-header {
    padding: 20px;
    background: #252525;
    border-bottom: 1px solid #333;
  }

  .chat-header h1 {
    margin: 0;
    font-size: 24px;
    color: #fff;
  }

  .chat-subtitle {
    margin: 4px 0 0;
    color: #888;
    font-size: 14px;
  }

  .chat-messages {
    flex: 1;
    overflow-y: auto;
    padding: 20px;
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .message {
    max-width: 85%;
    padding: 12px 16px;
    border-radius: 12px;
    line-height: 1.5;
  }

  .message.user {
    align-self: flex-end;
    background: #2563eb;
    color: #fff;
  }

  .message.assistant {
    align-self: flex-start;
    background: #333;
    color: #eee;
  }

  .message-content {
    white-space: pre-wrap;
  }

  .message-content ul {
    margin: 8px 0;
    padding-left: 20px;
  }

  .message-content li {
    margin: 4px 0;
  }

  .message.loading {
    background: #333;
    color: #888;
  }

  .chat-input-container {
    padding: 16px 20px;
    background: #252525;
    border-top: 1px solid #333;
  }

  .chat-form {
    display: flex;
    gap: 12px;
  }

  .chat-input {
    flex: 1;
    padding: 12px 16px;
    background: #1a1a1a;
    border: 1px solid #444;
    border-radius: 8px;
    color: #eee;
    font-size: 16px;
  }

  .chat-input:focus {
    outline: none;
    border-color: #2563eb;
  }

  .chat-input::placeholder {
    color: #666;
  }

  .chat-send-btn {
    padding: 12px 24px;
    background: #2563eb;
    color: #fff;
    border: none;
    border-radius: 8px;
    font-size: 16px;
    cursor: pointer;
    transition: background 0.2s;
  }

  .chat-send-btn:hover {
    background: #1d4ed8;
  }

  .chat-send-btn:disabled {
    background: #444;
    cursor: not-allowed;
  }

  /* Markdown-like formatting in responses */
  .message-content code {
    background: #1a1a1a;
    padding: 2px 6px;
    border-radius: 4px;
    font-family: monospace;
  }

  .message-content pre {
    background: #1a1a1a;
    padding: 12px;
    border-radius: 6px;
    overflow-x: auto;
    margin: 8px 0;
  }

  .message-content pre code {
    background: transparent;
    padding: 0;
  }

  /* Loading animation */
  .loading-dots {
    display: inline-flex;
    gap: 4px;
  }

  .loading-dots span {
    width: 8px;
    height: 8px;
    background: #888;
    border-radius: 50%;
    animation: bounce 1.4s infinite ease-in-out both;
  }

  .loading-dots span:nth-child(1) { animation-delay: -0.32s; }
  .loading-dots span:nth-child(2) { animation-delay: -0.16s; }

  @keyframes bounce {
    0%, 80%, 100% { transform: scale(0); }
    40% { transform: scale(1); }
  }
</style>

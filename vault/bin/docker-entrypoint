#!/bin/bash -e

# If running the rails server then create or migrate existing database
# Match both "./bin/rails server" and "bin/rails server" patterns
if [[ "$*" == *"rails"*"server"* ]]; then
  echo "Preparing database..."
  ./bin/rails db:prepare
fi

exec "${@}"
